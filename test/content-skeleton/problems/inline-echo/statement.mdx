<Constraints extra={{ 判题: "内联", 计分: "全对得满分" }} />

## 题目描述

把下面这个词原样提交上来。

<Callout kind="tip">答案是 `echo`。</Callout>

这道题存在的意义是让内联判题这条路径有一个活体消费者：内核在 `POST
/api/submissions` 的同一个事务里调用题目自己的 `judge` 函数，不入队、不等
runner。

<SubmitPanel />
