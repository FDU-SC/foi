# 界面设计约定

本文件记录现有题库界面的视觉约定。面向参赛者，便于浏览方向、选择题目和查看完成进度；沿用现有中文界面，不改变站点的语言配置。

## 样式来源

`app/globals.css` 是颜色、字体和主题的权威来源，通过 Tailwind 的 `@theme inline` 映射到工具类；`content/theme.css` 可覆盖颜色。本文不生成或复制颜色值。新增元素复用语义工具类，兼容浅色和深色主题。

## 字体与布局

沿用 Geist Sans 与系统字体回退；计数使用等宽数字。方向标题使用半粗体，说明与统计使用小号文字。题库分两栏；左栏选范围，右侧在该范围下用 tab 切换视图：

- 左侧题单栏：「全部题目」加按方向分组的题单；方向标题只作分组标签，下面的题单缩进；含多个题单的方向，缩进里第一项「全部」列出该方向的全部题目，用半粗体并以分隔线与下方分区隔开；窄屏收成一行可横向滚动的标签，不显示方向标题
- 右侧：当前题单标题，其下「题目」「排行榜」两个 tab，再是该视图的内容
  - 「题目」：说明、筛选器、题表和分页
  - 「排行榜」：时间范围、我的排名、用户搜索、榜单和分页

## 颜色、层次与形状

面板使用 `bg-surface`、`border-border`，标题区使用 `bg-surface-2/60`。文字通过 `text-fg`、`text-fg-muted`、`text-fg-subtle` 区分层次。面板沿用 `rounded-xl`，按钮沿用 `rounded-md`，进度条使用 `rounded-full`。不为进度条新增阴影或装饰图标。

## 进度与状态

题单栏的进度条由 `components/ui/list-nav.tsx` 内的 `Progress` 绘制，轨道使用 `bg-surface-3`，填充使用 `bg-primary`，高度为 `h-1.5`。

- 位置：每个题单一条，含「全部题目」和各方向的「全部」
- 计数：仅显示“完成数 / 总题数”，不加“已通过”前缀；同时提供读屏可识别的名称、当前值和总数
- 统计：规则遵循 `lib/problems/selection.ts`；只汇总可见题单，缺少完整进度或没有题目时隐藏进度条；跨题单的同题按各自所属比赛分别统计
- 窄屏只保留计数，不画进度条

## 交互与加载

保留现有链接、tab 和焦点样式。进度条是静态信息，不增加动画或客户端请求。界面文案遵循 `AGENTS.md`，不展示配置或实现细节。

加载状态全站统一：

- 常驻外壳内切换：不挂骨架，上一个面板保留到下一个就绪，只有正文淡入淡出
  - 题库：左栏、标题与 tab 由 `(catalogue)/(shell)` layout 保持挂载
  - 比赛工作区：顶栏与题单侧栏由 `(workspace)` layout 保持挂载
- 进入外壳或整页跳转：显示该页骨架（`loading.tsx`，随链接预取）
  - 骨架放在 `views/skeletons/`，外层用 `SkeletonScreen`，读屏文案形如“正在加载题库”
  - 固定的页面标题显示真实文字，动态内容用灰块
  - `loading.tsx` 只包住一个页面或一个外壳；页面有子路由时移入 `(index)` 路由组，避免父级骨架先出现
- 页面内分块加载（首页面板）：`Suspense`，fallback 同样用 `SkeletonScreen`
- 等待操作结果：按钮 `pending`，禁用并显示 `Spinner`，文案形如“提交中…”
- 后台进行中的状态（评测中、自动刷新、评测机在线）：`PulseDot`
- 动效：循环动画仅在未要求减少动效时播放；过渡曲线统一为 `cubic-bezier(0.16, 1, 0.3, 1)`
