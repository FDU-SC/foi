[Unit]
Description=定时检查演示站是否有新镜像
Documentation=https://github.com/FDU-SC/foi

[Timer]
# 每小时查一次。频率决定的是「新镜像发现后多久生效」，不是重建频率——镜像一天才
# 换一次，摘要没变的检查会立刻退出。
OnCalendar=hourly
# 错开整点：那是 CI 与各类定时任务扎堆的时刻。
RandomizedDelaySec=5min
# 机器停过一段时间的话，开机后补一次。
Persistent=true

[Install]
WantedBy=timers.target
