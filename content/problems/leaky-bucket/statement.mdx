import { InstanceControl } from "./InstanceControl";
import { Callout } from "@/content/_shared/mdx/callout";
import { Constraints } from "@/content/_shared/mdx/constraints";
import { SubmitPanel } from "@/content/_shared/ui/submit-panel";
import { FlagInput } from "@/content/_shared/ui/flag-input";

<Constraints extra={{ 分类: "Web", 初始分值: "300", 计分: "动态" }} />

某内部服务的优惠券兑换接口要求每个账号每分钟只能兑换一次，
但限流 key 的实现有误，可以绕过。

找出绕过方法，使优惠券余额变为负数，服务会在响应头中返回 flag。

<InstanceControl />

## 提示

<Callout kind="note">

限流器的 key 由若干请求特征拼接而成。仔细观察哪些特征是你能够控制的。

</Callout>

<Callout kind="warning">

请不要对靶机发起高并发压测。这道题不需要爆破，暴力请求只会让实例被回收。

</Callout>

## 提交格式

flag 形如 `FOI{...}`，直接粘贴到下方提交框。

<SubmitPanel><FlagInput /></SubmitPanel>
