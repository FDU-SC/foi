# 这套 content 需要的环境变量。
#
# 不是平台模板——平台那份在仓库根的 .env.example，里面只有命名约定，没有一个
# 后端的名字。名字属于这里：`content/backends.ts` 声明了哪些后端，就有哪些
# 变量，换一套 content 这份文件跟着一起换。
#
# 这四个名字曾经同时抄在 .env.example、.env.production.example、
# setup-deploy.sh 和 CI 的冒烟步骤里。加一个后端要记得改四处，而把 content/
# 换掉之后，那四处描述的是一套不存在的部署。现在前三处读
# `scripts/list-backends.ts`，这份文件是给人看的那一份。
#
# 变量名的拼法：id 大写、连字符换下划线。`leaky-bucket` → `LEAKY_BUCKET`。

# ── 签名密钥 ────────────────────────────────────────────────────────────────
# 每个后端一把，覆盖 FOI_BACKEND_SECRET。生产环境下两个有流量的后端共用回落
# 密钥会拒绝启动：这把钥匙现在是 runner 用来认证「入站」的，握着它就等于握着
# 那条队列——读得到里面每一份提交，也写得进任意评测结果。
# 各生成一个：openssl rand -hex 32
#FOI_BACKEND_TRADITIONAL_SECRET=
#FOI_BACKEND_INTERACTIVE_SECRET=
#FOI_BACKEND_PERFORMANCE_SECRET=
#FOI_BACKEND_LEAKY_BUCKET_SECRET=

# ── 地址 ────────────────────────────────────────────────────────────────────
# 只有「有题目在它上面声明了 actions」的后端才需要地址，这套 content 里就是
# leaky-bucket 一个：它发靶机，`spawn`/`poll`/`destroy` 是选手同步等结果的请求，
# 拉不了。其余三个只判题，runner 自己来找 FOI_PUBLIC_URL，平台不连它们。
#
# 开发环境下没配地址的条目一律回落到 FOI_DEV_BACKEND_URL（见根目录
# .env.example），所以本地 clone 只要 `pnpm backend:mock` 就跑得通。
#FOI_BACKEND_LEAKY_BUCKET_URL=http://localhost:4105

# 退役的三台不在这里：flag-checker、output-only、roulette 现在是
# content/problems/_shared/judge/ 里的内联判题，不需要地址、密钥和部署。
